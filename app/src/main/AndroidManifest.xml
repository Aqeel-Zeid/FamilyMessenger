<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.familymessenger">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddAlert"></activity>
        <activity android:name=".AlertMain" />
        <activity android:name=".MainMenu" />
        <activity android:name=".FamilyMemberUpdateView" />
        <activity android:name=".CreateMember" />
        <activity android:name=".FamilyMainMenu" />
        <activity android:name=".CreateFamily" />
        <activity android:name=".FamilyManager" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>